public class Rumpang {
    public static void main(String[] args) {
        System.out.println("--- Soal 6: Angka 11 Dua Kali ---");
        
        for (int i = 0; i < 10; i++) {
            // Bagian yang rumpang: Cek jika i adalah 0 atau 5
            if (i == 0 || i == 5) {
                System.out.println(11);
            }
            // Tambahkan baris ini jika ingin melihat i berjalan:
            // System.out.println("i saat ini: " + i); 
        }
    }
}
